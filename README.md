# TSP_Main
Algoritmo de detección del tipo de sonrisa de una persona usando redes neuronales
Antes de ejecutar el proyecto es necesario instalar las librerias usadas, para ello ejecutar el siguiente comando en su terminal  

pip install -r requirements.txt
